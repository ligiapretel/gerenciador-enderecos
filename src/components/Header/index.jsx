import './header.css';

function Header(){
    return(
        <header>
            <a href="#">Usuários</a>
            <a href="#">Cadastrar usuário</a>
            <a href="#">Cadastrar endereço</a>
        </header>
    );
};

export default Header;